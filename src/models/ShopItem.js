class ShopItem {
    constructor(values) {
        this.brand = values.brand;
        this.title = values.title;
        this.description = values.description;
        this.descriptionFull = values.descriptionFull;
        this.price = values.price;
        this.currency = values.currency;
    }
}

export default ShopItem;